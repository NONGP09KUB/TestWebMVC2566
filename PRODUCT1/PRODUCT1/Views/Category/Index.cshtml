@model IEnumerable<PRODUCT1.Models.Category>

@{
    ViewData["Title"] = "Index";
}

@* @if (TempData["Error"] != null )
{
   @*  <div class="alert alert-dismissible alert-secondary">
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        <strong> ไม่มี </strong> ข้อมูล <a href="#" class="alert-link">@TempData["Error"]</a>.
    </div> *@
   @*  <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="//cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/toastr.min.js"></script>
    <script type="text/javascript">
        toastr.warning('@TempData["Error"]')
    </script>
} *@ 
<h1>Index</h1>

<p>
    <a asp-action="Upcreate">Create New</a>
</p>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreateDateTime)
            </th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreateDateTime)
                </td>
                <td>
                    <a asp-action="Upcreate" asp-route-id="@item.Id">Edit</a>
                </td>
                <td>
                    <a asp-action="Delete" asp-route-id="@item.Id"
                    onclick="confirm('คุณจะลบจริง ๆ หรือ ห้ะะะะะ')">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>

